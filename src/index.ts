/**
 * dsh-thinkbar node half. This UI-only plugin needs a host Loader row so the
 * DSH client-module scanner can discover its browser bundle.
 */

/** Host plugin body: dsh-thinkbar has no host-side behavior. */
export function apply(): void {}
